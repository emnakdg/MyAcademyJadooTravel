@model JadooTravel.Dtos.ServiceDtos.GetServiceByIdDto
@{
    Layout = "~/Views/AdminLayout/Layout.cshtml";
    ViewData["Title"] = "Hizmet Güncelle";
}

<div class="body-wrapper">
    <div class="body-wrapper-inner">
        <div class="container-fluid">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title fw-semibold mb-4">Hizmet Güncelle</h5>
                    <form method="post">
                        <input type="hidden" asp-for="ServiceId" />
                        <div class="mb-3">
                            <label class="form-label">Hizmet Başlığı</label>
                            <input type="text" asp-for="Title" class="form-control" required />
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Açıklama</label>
                            <textarea asp-for="Description" rows="3" class="form-control"></textarea>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">İkon URL</label>
                            <input type="text" asp-for="IconUrl" class="form-control" />
                        </div>
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="ti ti-check"></i> Güncelle
                            </button>
                            <a href="/Service/ServiceList" class="btn btn-secondary">
                                <i class="ti ti-x"></i> İptal
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
