@model JadooTravel.Dtos.TestimonialDtos.GetTestimonialByIdDto
@{
    Layout = "~/Views/AdminLayout/Layout.cshtml";
    ViewData["Title"] = "Referans Güncelle";
}

<div class="body-wrapper">
    <div class="body-wrapper-inner">
        <div class="container-fluid">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title fw-semibold mb-4">Referans Güncelle</h5>
                    <form method="post">
                        <input type="hidden" asp-for="TestimonialId" />
                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label">Ad Soyad</label>
                                    <input type="text" asp-for="NameSurname" class="form-control" required />
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label">Şehir/Ülke</label>
                                    <input type="text" asp-for="CityCountry" class="form-control" />
                                </div>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Fotoğraf URL</label>
                            <input type="text" asp-for="ImageUrl" class="form-control" />
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Yorum</label>
                            <textarea asp-for="Comment" rows="4" class="form-control" required></textarea>
                        </div>
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="ti ti-check"></i> Güncelle
                            </button>
                            <a href="/Testimonial/TestimonialList" class="btn btn-secondary">
                                <i class="ti ti-x"></i> İptal
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
